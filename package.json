{
  "name": "my_hello_world",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "genesis": "npx tsc && node config/createGenesisBlock.js",
    "start": "npx tsc && node dist/index.js",
    "unsafe-reset": "rm -rf ~/.lisk/TrustSECO-DLT",
    "reset": "read -p \"Removing ~/.lisk/.. of the CURRENT USER! Are you sure? [yes|no] \" ok && if [ \"$ok\" = \"yes\" ]; then npm run unsafe-reset; fi;"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@liskhq/lisk-framework-dashboard-plugin": "^0.1.6",
    "lisk-sdk": "^5.2.1"
  },
  "devDependencies": {
    "prompt": "^1.3.0",
    "typescript": "^4.6.4"
  }
}
