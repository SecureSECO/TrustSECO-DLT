{
  "name": "my_hello_world",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "genesis": "npm run build && node config/createGenesisBlock.js",
    "start": "npm run build && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@liskhq/lisk-framework-dashboard-plugin": "^0.1.6",
    "lisk-sdk": "^5.2.1"
  },
  "devDependencies": {
    "prompt": "^1.3.0",
    "typescript": "^4.6.4"
  }
}
